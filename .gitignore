node_modules
.svelte-kit
build
.env
.env.local
.DS_Store
*.log
dist
# Cursor IDE local state (keep shared rules under .cursor/rules/)
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.serena
.agents
skills-lock.json

# Supabase CLI local cache (link state, temp during db pull/push)
supabase/.temp
